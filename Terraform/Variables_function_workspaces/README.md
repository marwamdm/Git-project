In this folders, we are going to see how to deploy in multiple environments 
(dev, uat and production) using the same configuration. depending on which variable is set,
de deployment will be diferent
